# 08_reporting — Reporting & PRISMA

- **`prisma_flow_counts.csv`** — the running counts that build the PRISMA 2020 flow
  diagram. Populated from `../02_search/` (identification, dedup) and `../03_screening/`
  (screened, excluded, included). Consumed by `../06_analysis/R/10_prisma_flow.R`.
- **`PRISMA2020_checklist.md`** — the 27-item checklist for the completed review.
- The **protocol's** PRISMA-P 2015 compliance lives in the protocol's Appendix A.

OSF deposit (code + materials) is planned prior to publication — see
`../10_admin/OSF_deposit_plan.md`.
